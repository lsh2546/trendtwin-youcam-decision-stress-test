# GitHub publishing handoff

Recommended repository name: `trendtwin-youcam-decision-stress-test`

Recommended description:

> Counterfactual fashion decision stress testing powered by YouCam Apparel VTO.

Before publishing:

1. Create a public GitHub repository without initializing it with generated files.
2. Upload the contents of `trendtwin-github-source.zip`.
3. Confirm `.env` is absent and `.env.example` is present.
4. Confirm the README hero loads and the MIT license is visible.
5. Run the fresh-clone commands from README.
6. Add the final repository URL to Devpost.

Suggested topics:

`youcam-api`, `virtual-try-on`, `fashion-tech`, `counterfactual`, `retail-ai`, `react`, `typescript`, `hackathon`
